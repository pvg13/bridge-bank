services:
  bridge-bank:
    image: daalves/bridge-bank:latest
    container_name: bridge-bank
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
      - ./app:/app/app
    ports:
      - "3002:3000"
